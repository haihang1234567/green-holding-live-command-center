import {useEffect,useState} from 'react'
import {api} from '../api'
import AdminHeader from '../components/AdminHeader'
export default function AdminUsers(){const [rows,setRows]=useState([]),[dir,setDir]=useState(null),[form,setForm]=useState({username:'',password:'',role:'TEAM',team_id:1}),[msg,setMsg]=useState('');const load=()=>Promise.all([api.users(),api.directory()]).then(([u,d])=>{setRows(u);setDir(d)});useEffect(()=>{load()},[]);const create=async()=>{try{await api.createUser({...form,team_id:form.role==='TEAM'?Number(form.team_id):null});setForm({...form,username:'',password:''});setMsg('Đã tạo tài khoản');load()}catch(e){setMsg(e.message)}};return <><AdminHeader title="Người dùng" subtitle="Admin xem toàn bộ; Team User chỉ xem team được gán"/><div className="user-admin-grid"><div className="panel"><div className="panel-title"><h3>Tạo tài khoản</h3></div><div className="form-grid"><label>Username<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>TEAM</option><option>ADMIN</option></select></label>{form.role==='TEAM'&&<label>Team<select value={form.team_id} onChange={e=>setForm({...form,team_id:e.target.value})}>{dir?.teams.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</select></label>}<button className="primary-btn" onClick={create}>Tạo tài khoản</button>{msg&&<small>{msg}</small>}</div></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Username</th><th>Role</th><th>Team</th><th>Trạng thái</th><th></th></tr></thead><tbody>{rows.map(u=><tr key={u.id}><td><b>{u.username}</b></td><td>{u.role}</td><td>{u.team_name||'—'}</td><td>{u.active?'Hoạt động':'Đã khóa'}</td><td><button className="secondary-btn" onClick={async()=>{await api.setUserActive(u.id,!u.active);load()}}>{u.active?'Khóa':'Mở khóa'}</button></td></tr>)}</tbody></table></div></div></div></>}
