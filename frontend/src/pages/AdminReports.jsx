import {useEffect,useState} from 'react'
import {api} from '../api'
import {money,num,pct} from '../utils'
import AdminHeader from '../components/AdminHeader'
import SnapshotSelect from '../components/SnapshotSelect'
export default function AdminReports(){const [snap,setSnap]=useState('T3H'),[from,setFrom]=useState(''),[to,setTo]=useState(''),[data,setData]=useState(null),[busy,setBusy]=useState('');const qs=()=>{const p=new URLSearchParams();p.set('snapshot_type',snap);if(from)p.set('date_from',from);if(to)p.set('date_to',to);return `?${p.toString()}`};const load=()=>api.report(qs()).then(setData);useEffect(()=>{load()},[snap]);const exp=async(type)=>{setBusy(type);try{type==='xlsx'?await api.exportExcel(qs()):await api.exportPdf(qs())}finally{setBusy('')}};return <><AdminHeader title="Báo cáo" subtitle="Theo ngày / tuần / tháng / team / kênh / phiên LIVE" right={<SnapshotSelect value={snap} onChange={setSnap} compact/>}/><div className="report-filter panel"><label>Từ ngày<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>Đến ngày<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><button className="primary-btn" onClick={load}>Xem báo cáo</button><button className="secondary-btn" onClick={()=>exp('xlsx')}>{busy==='xlsx'?'Đang xuất...':'Xuất Excel'}</button><button className="secondary-btn" onClick={()=>exp('pdf')}>{busy==='pdf'?'Đang xuất...':'Xuất PDF'}</button></div>{data&&<><div className="kpi-grid five"><div className="kpi-card"><div className="kpi-label">GMV</div><div className="kpi-value">{money(data.totals.gmv)}</div></div><div className="kpi-card"><div className="kpi-label">Net Revenue</div><div className="kpi-value">{money(data.totals.net_revenue)}</div></div><div className="kpi-card"><div className="kpi-label">Orders</div><div className="kpi-value">{num(data.totals.orders)}</div></div><div className="kpi-card"><div className="kpi-label">Refund Rate</div><div className="kpi-value">{pct(data.totals.refund_rate)}</div></div><div className="kpi-card"><div className="kpi-label">ROAS</div><div className="kpi-value">{data.totals.roas}</div></div></div><div className="panel"><div className="table-wrap"><table><thead><tr><th>Session</th><th>Ngày</th><th>Team</th><th>Kênh</th><th>Ca</th><th>GMV</th><th>Net</th><th>Orders</th><th>Refund</th><th>Top SKU</th></tr></thead><tbody>{data.rows.map(r=><tr key={r.session_code}><td><b>{r.session_code}</b></td><td>{r.date}</td><td>{r.team}</td><td>{r.channel}</td><td>{r.shift}</td><td>{money(r.gmv)}</td><td>{money(r.net_revenue)}</td><td>{num(r.orders)}</td><td>{pct(r.refund_rate)}</td><td>{r.top_sku}</td></tr>)}</tbody></table></div></div></>}</>}
