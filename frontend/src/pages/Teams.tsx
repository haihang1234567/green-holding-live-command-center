import { useEffect, useState } from 'react'
import { api } from '../lib/api'
import { compactMoney, money, number, percent } from '../lib/format'

export default function Teams(){const[data,setData]=useState<any>(null);useEffect(()=>{api('/dashboard/overview').then(setData)},[]);if(!data)return <div className="page-loader">Đang tải...</div>;return <><div className="page-title"><div><span className="section-kicker">TEAM PERFORMANCE</span><h2>Xếp hạng 4 team</h2><p>So sánh GMV, đơn, AOV, Ads, hoàn/hủy và hiệu suất theo giờ</p></div></div><div className="ranking-cards">{data.ranking.map((x:any,i:number)=><article className="rank-card" key={x.team_id}><div className="rank-no">{i===0?'🥇':i===1?'🥈':i===2?'🥉':`#${i+1}`}</div><div><small>TEAM</small><h3>{x.team_name}</h3></div><strong>{compactMoney(x.gmv)}</strong><span>GMV</span></article>)}</div><div className="panel table-panel"><div className="table-scroll"><table><thead><tr><th>RANK</th><th>TEAM</th><th>GMV</th><th>ORDERS</th><th>AOV</th><th>ADS</th><th>ADS/GMV</th><th>REFUND RATE</th><th>NET REVENUE</th><th>GMV/HOUR</th></tr></thead><tbody>{data.ranking.map((x:any,i:number)=><tr key={x.team_id}><td><b>#{i+1}</b></td><td><b>{x.team_name}</b></td><td>{money(x.gmv)}</td><td>{number(x.orders)}</td><td>{money(x.aov)}</td><td>{money(x.ads)}</td><td>{percent(x.ads_percentage)}</td><td>{percent(x.refund_rate)}</td><td>{money(x.net)}</td><td>{money(x.gmv_per_hour)}</td></tr>)}</tbody></table></div></div></>}
